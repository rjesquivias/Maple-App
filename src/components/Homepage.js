import Header from "./Header";
import NavCards from "./NavCards";

const Homepage = () => {
    return (
        <>
        <Header />
        <NavCards />
        </>
    );
};

export default Homepage;